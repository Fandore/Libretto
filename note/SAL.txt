16/07/2026 — Sessione 12c: Reminder nel calendario (v22.12) — LIVE ✅
SAL:
- state.reminders[] — array separato da transactions (automaticamente escluso da tutti i calcoli)
  Struttura: {id, date, payee, category, amount, note}
  migrateState: guard `if(!state.reminders) state.reminders=[]`
- openReminderModal(dateStr): form semplificato (importo, data, descrizione, categoria, nota)
- openReminderActionModal(reminderId): popup su click reminder
  - "Registra come spesa": apre openTransactionModal pre-compilato; al salvataggio rimuove il reminder
  - "Elimina reminder": rimuove da state.reminders
- openTransactionModal: aggiunto parametro `prefill` (usato da conversione reminder)
  - src = existing || prefill || {} — backward compatible, tutti i caller esistenti invariati
- pendingReminderConvert: variabile modulo per tracciare la conversione; resettata dopo il salvataggio
- renderCalendar(): aggiornato con:
  - remByDay: reminder raggruppati per giorno del mese
  - cal-ev-reminder (viola tratteggiato) / cal-ev-reminder-overdue (rosso tratteggiato)
  - Tooltip su ogni reminder con payee + nota
  - Totale giornaliero include anche importi reminder
  - Max 2 ricorrenti visibili se ci sono reminder (slot per entrambi)
  - Banner alert se ci sono reminder scaduti (data < oggi)
  - Pulsante "+ Reminder" nel topbar
- bindPageEvents: handler addReminderBtn + click delegation su [data-reminder-id]
- CSS: .cal-ev-reminder, .cal-ev-reminder:hover, .cal-ev-reminder-overdue, .cal-ev-reminder-overdue:hover
- APP_VERSION: 22.12-reminders

16/07/2026 — Sessione 12b: Fix dashboard filtro conto (v22.11) — LIVE ✅
SAL:
- Bug fix: selectedDashboardAccountId() non gestiva il valore 'all' → selezionando "Tutti i conti"
  ricadeva sempre sul primo conto (state.accounts.some(a=>a.id==='all') === false)
- Fix: aggiunto guard `if(id==='all') return 'all'` prima del check sugli account
- Default cambiato da primo conto a 'all' (migrateState: dashboardAccountId='all' se undefined)
- accountBalancesCardHTML() ora appare solo quando selectedAccount==='all' (ridondante su conto singolo)
- APP_VERSION: 22.11-dashboard-fix

16/07/2026 — Sessione 12: Rischedule mail riepilogo — LIVE ✅
SAL:
- Spostati orari invio mail per dare tempo di inserire le ultime spese:
  - Settimanale: lunedì 08:00 UTC → martedì 08:00 UTC (10:00 IT) — cron: 0 8 * * 2
  - Riepilogo mensile: 1° del mese 08:00 UTC → 5° del mese 08:00 UTC — cron: 0 8 5 * *
  - Promemoria scadenze: 1° del mese 07:00 UTC → 5° del mese 07:00 UTC — cron: 0 7 5 * *
- cron_setup.sql aggiornato con le nuove schedule + sezione "AGGIORNAMENTO SCHEDULE" con i comandi unschedule/reschedule
- Job aggiornati live via SQL Editor (unschedule + reschedule confermati con select * from cron.job)
- Pendente: deploy send-monthly-reminder (prima esecuzione live sarà il 5/08/2026)

15/07/2026 — Sessione 11c: Mail promemoria scadenze mensili — IMPLEMENTATO (deploy manuale richiesto)
SAL:
- Nuova Edge Function send-monthly-reminder (supabase/functions/send-monthly-reminder/index.ts)
  - Si attiva ogni 1° del mese alle 07:00 UTC (un'ora prima del riepilogo send-report)
  - Per ogni utente: legge transactions con recurring:true dallo state Supabase
  - Proietta le scadenze nel mese corrente con la stessa logica di recurringDaysInMonth() dell'app:
    * Mensile: giorno da recurringNextDate o dalla data originale della transazione
    * Settimanale: itera di 7 giorni da nextDate per trovare tutte le occorrenze nel mese
    * Bimestrale/Annuale: verifica allineamento con nextDate; non include se il mese non batte
    * Deduplication: chiave payee|category|tipo (prende solo la più recente per combinazione)
  - Separa uscite (type=out) e risparmi (movementType=transfer) in sezioni distinte
  - Calcola stipendio medio (ultimi 3 isSalaryTx) per mostrare il disponibile
  - HTML email: 4 card summary (Stipendio / Uscite fisse / Risparmi / Disponibile netto)
    + tabella uscite con giorno/descrizione/categoria/freq/importo + sezione risparmi verde
    + box colorato con importo disponibile stimato
- cron_setup.sql: aggiunto job 'libretto-monthly-reminder' (0 7 1 * *)
  + entry test manuale e unschedule per il nuovo job
- note/ChatGPT_requests.txt: feature 6 aggiornata con nota v22.10

DEPLOY manuale richiesto:
  supabase functions deploy send-monthly-reminder --project-ref marvmbewsgxrabirugkk
  Poi eseguire in Supabase SQL Editor il nuovo select cron.schedule('libretto-monthly-reminder', ...)

15/07/2026 — Sessione 11b: Risparmi programmati nelle proiezioni (v22.10) — LIVE ✅
SAL:
- fixedExpenses(): include ora i trasferimenti con recurring=true come {isSaving:true}
  - Chiave deduplicazione: payee|account|saving (separata da spese normali)
  - Importo: non usa avgLastNAmounts (non è una spesa variabile, è sempre lo stesso importo)
- fixedExpensesForMonthSplit(projDate): nuovo helper che restituisce {expenses, savings}
  - Stessa logica di fixedExpensesForMonth ma biforcata per isSaving
- projectionRows(): ora usa split; saldo totale cresce solo di (income-expenses) perché il
  risparmio resta in patrimonio; netFree = income-expenses-savings = liquidità vera
  - Colonna "net" nel row = netFree (cambiamento semantico, era income-fixed)
  - Colonna "balance" = patrimonio totale (corretto, non influenzato dai risparmi)
- renderAnalytics() tabella proiezione: colonna "Risparmi prog." aggiunta condizionalmente
  (appare solo se almeno un trasferimento ricorrente è presente). Label "Netto libero" invece
  di "Netto stimato" quando ci sono risparmi. Note esplicativa sotto la tabella.
- renderSubscriptions(): sezione "🏦 Risparmi programmati" separata con tabella sage-colored
  + KPI quarto "Risparmi programmati / mese"
- recurringSummaryHTML() dashboard: sezione risparmi in fondo alla card ricorrenti
- CSS: .sub-saving-payee (sage), .sub-savings-note (box verde trasparente con bordo sage)
- APP_VERSION: 22.10-savings

15/07/2026 — Sessione 11: Abbonamenti (F12) + Confronto cicli (F13) + Calendario (F14) (v22.9) — LIVE ✅
SAL:
- Feature 12 — renderSubscriptions() (pagina "🔁 Abbonamenti"):
  - KPI row: ricorrenti attive / costo mensile totale / costo annuale totale
  - Tabella ricorrenti attive: esercente, categoria, frequenza badge, importo, ≈/mese, ≈/anno, prossima data, pulsante ⏹ Stop
  - Sezione ricorrenti sospese con pulsante ▶ Riattiva (riusa data-stop-rec / bindPageEvents esistente)
  - Dati da fixedExpenses() + stoppedRecurringsDetails(); importi mensili via recurringMonthlyEquivalent()
- Feature 13 — Confronto ciclo corrente vs ciclo precedente (in fondo a renderAnalytics()):
  - Tabella per categoria: spesa ciclo corrente | spesa ciclo precedente | Δ€ con % freccia ▲▼
  - Riga totale in <tfoot> con totale uscite e variazione %
  - Usa prevCycleBounds() (aggiunto sessione 10) + txInBounds() + spentByCategory()
  - Graceful degradation: se meno di 2 stipendi registrati mostra messaggio informativo
- Feature 14 — renderCalendar() (pagina "📅 Calendario"):
  - Griglia 7 colonne (Lun–Dom), navigazione prev/next mese con calViewYear/calViewMonth
  - Giorno stipendio: numero verde + event card "💰 Stipendio" (da salaryDates() o settings.salaryDay)
  - Spese ricorrenti per giorno: date calcolate da recurringDaysInMonth() per tutte fixedExpenses()
  - Totale giornaliero in basso a destra di ogni cella, al massimo 3 event card + "+N altro"
  - Oggi evidenziato con bordo gold
- Navigazione sidebar: aggiunti 🔁 Abbonamenti e 📅 Calendario tra Analisi e Obiettivi (index.html)
- render(): aggiunti case 'subscriptions' e 'calendar'
- bindPageEvents(): aggiunti handler calPrev/calNext
- APP_VERSION: 22.9-features | WHATS_NEW: messaggio per le 3 nuove sezioni
- CSS: .sub-table-wrap, .sub-table, .sub-payee, .sub-freq, .sub-stop-btn, .cmp-table tfoot,
       .cal-grid, .cal-cell, .cal-head, .cal-day-num, .cal-event, .cal-ev-salary, .cal-ev-exp,
       .cal-ev-more, .cal-day-total, .cal-legend, responsive overrides @760px

15/07/2026 — Sessione 10: Parser IsyBank + "Novità" banner (v22.8) — LIVE ✅
SAL:
- Aggiunto formato IsyBank in src/bankImport.js:
  - csvToRowsComma(): parser CSV con virgola come delimitatore (vs punto e virgola di ING)
  - ISYBANK_CATEGORY_MAP: mapping categorie IsyBank → categorie default Libretto
    (es. "Ristoranti e bar"→Ristoranti, "Generi alimentari e supermercato"→Spesa, "Farmacia"→Salute)
  - parseIsyBankRows(): rileva header row con row[0]==="Data" (dopo N righe di metadata account)
    - Colonne: Data | Operazione | Dettagli | Conto o carta | Contabilizzazione | Categoria | Valuta | Importo
    - Importo: plain float con punto decimale (non formato italiano), signed (negativo=uscita)
    - categoryHint: passa la categoria banca al wizard per auto-suggerimento
    - rawDescription: campo Dettagli usato per il tooltip sull'esercente
  - parseIsyBank(): branch CSV (csvToRowsComma) vs XLSX (xlsxToRows) come ING
  - BANK_FORMATS aggiornato con entry IsyBank + categoryMap
- main.js openBankImportStep2: logica categoryHint
  - Se suggestCategoryForPayee(payee) non trova match, controlla r.categoryHint via fmt.categoryMap
  - Verifica che la categoria mappata esista in catNames() prima di assegnarla
  - Risultato: le spese IsyBank mostrano ✅ pronto con categoria già mappata (es. Farmacia→Salute)
- APP_VERSION aggiornata a 22.8-isybank
- Banner "Novità" (showWhatsNew):
  - Costante WHATS_NEW in main.js: testo HTML aggiornabile ad ogni deploy
  - state._meta.lastSeenVersion: traccia l'ultima versione vista dall'utente
    - Nuovi utenti: lastSeenVersion=APP_VERSION in init → banner mai mostrato
    - Utenti esistenti: lastSeenVersion!=APP_VERSION → banner mostrato una volta sola
  - showWhatsNew(): snackbar fisso in basso, sparisce dopo 10s o al click ✕
  - CSS in app.css: #whatsNewBanner con animazione slideUp, border-left sage
  - Per ogni deploy: aggiornare APP_VERSION e WHATS_NEW (una riga in main.js)
- Formato CSV IsyBank: 7 righe metadata → header riga 8 → dati (con molte righe vuote finali)
- Formato XLSX IsyBank: stesso schema colonne, xlsxToRows gestisce automaticamente

--- STORICO ---
15/07/2026 — Sessione 9: Import da homebanking (v22.7) — LIVE ✅
SAL:
- req. 10: Wizard import movimenti da file CSV banca
  - Nuovo modulo src/bankImport.js: registry BANK_FORMATS + parser ING Italia
    - parseITAmount: gestisce formato IT (1.234,56), parseING: CSV `;`-delimitato
    - extractPayeeING: estrae esercente dalla DESCRIZIONE per tipo CAUSALE
      (Pagamento Carta → dopo "presso"; Addebito Diretto → tra Creditor id. e Id Mandato;
       Bonifico In Uscita → dopo "A favore di"; Accredito Bonifico → dopo "Anagrafica Ordinante")
    - Salta righe senza CAUSALE (Saldo iniziale / Saldo finale)
    - Usa DATA VALUTA come data transazione (fallback DATA CONTABILE)
  - state.merchantMappings: {} — nuovo campo persistito per memorizzare payee→category
  - migrateState: guard `if(!state.merchantMappings) state.merchantMappings={}`
  - suggestCategoryForPayee(payee): cerca prima in merchantMappings, poi in transazioni esistenti (moda)
  - isDuplicateBankTx(date,amount,type,payee,accountId): rileva duplicati per (data,importo,tipo,esercente,conto)
  - openBankImportWizard(): Step 1 — selezione formato banca + conto + carica file
  - openBankImportStep2(): Step 2 — tabella revisione con:
    - Riga ✅ pronto: categoria auto-suggerita (nascosta in input hidden)
    - Riga ⚠️ nuovo: dropdown selezione categoria + checkbox 💾 "Ricorda esercente"
    - Riga 🔄 duplicato: opacità ridotta, deselezionato di default
    - Contatore live movimenti da importare
    - Al conferma: salva merchantMappings per le righe con "Ricorda" checked, inserisce tx, sort, toast
  - Bottone "🏦 Importa banca" aggiunto in topbar pagina Movimenti
  - CSS: .bi-status (ok/new/dup), .bi-payee-cell, responsive media query
  - APP_VERSION aggiornata a 22.7-bank-import
- Fix debug sessione (CSV + XLSX):
  - BOM handling più robusto (strip per-riga, non solo inizio testo)
  - parseITAmount: gestisce JS number diretto (da Excel) senza stringificare
  - xlsxToRows: usa cellDates:true → Date objects convertiti a DD/MM/YYYY
  - itDateToISO: accetta anche D/M/YYYY e YYYY-MM-DD oltre DD/MM/YYYY
  - parseINGRows: colonne dinamiche via findIndex sull'header (non più hardcoded [0]-[5])
  - Scoperto: formato ING Excel ≠ CSV
    CSV:   DATA CONTABILE | DATA VALUTA | USCITE | ENTRATE | CAUSALE | DESCRIZIONE OPERAZIONE
    Excel: DATA CONTABILE | DATA VALUTA | CAUSALE | DESCRIZIONE OPERAZIONE | IMPORTO IN EURO (header riga 12)
  - Parser ora gestisce entrambi i formati automaticamente (branch iImporto vs iUscite/iEntrate)
- Bottone "+ Richiedila" nel wizard Step 1: apre mailto pre-compilata all'admin
  con istruzioni per inviare file di esempio anonimizzato per richiedere nuovi formati banca
- Architettura BANK_FORMATS registry: per aggiungere un nuovo formato basta aggiungere un entry con { id, label, accept, parse } in src/bankImport.js

--- STORICO ---
13/07/2026 — Sessione 8: Tutorial onboarding (v22.6)
SAL:
- Aggiunto tutorial walkthrough a 7 step per i nuovi utenti:
  - state.tutorialSeen (bool) aggiunto allo state + migrazione in migrateState()
  - Costante TUTORIAL_STEPS (array 7 oggetti: icon, title, text, page)
  - Funzione openTutorialModal(step=0): modal multi-step con Avanti/Indietro/Salta
  - Auto-trigger al primo utilizzo: if(!state.tutorialSeen) openTutorialModal(0) in init()
  - Step 7 (ultimo): CTA "Inizia dai Conti →" che chiude e naviga su accounts
  - Salta / chiusura X → state.tutorialSeen=true, saveState()
- Tasto "❓ Tour guidato" aggiunto in sidebar (index.html), fuori dal <nav>
  - Al click apre il tutorial dallo step corrispondente alla pagina corrente
  - Mappa pagina→step: accounts:1, transactions:2, dashboard:3, budget:4, goals:5, analytics:6
  - Chiude il menu mobile al click
- APP_VERSION aggiornata a 22.6-tutorial

--- STORICO ---
13/07/2026 — Sessione 7: Importi ricorrenti variabili (v22.5) + Deploy mail riepilogo (v22.4) — LIVE ✅
SAL:
- req. 7: Aggiunta funzione avgLastNAmounts(payee, category, n=3) in src/main.js
  - Calcola la media degli ultimi N importi per payee+categoria (default 3)
  - fixedExpenses() ora usa avg??t.amount per le ricorrenti manuali
  - recurringSummaryByCategory() aggiornata di conseguenza
  - Impatto: proiezioni cashflow e riepilogo ricorrenti usano la media reale invece dell'ultimo importo fisso
- APP_VERSION aggiornata a 22.5-variable-recurring

--- STORICO ---
13/07/2026 — Sessione 7: Deploy mail riepilogo (v22.4) — LIVE ✅
SAL:
- Deploy infrastruttura completo:
  - GitHub Actions workflow: .github/workflows/deploy-functions.yml
  - Edge Function deployata via GitHub Actions (supabase/setup-cli)
  - Secrets Supabase impostati: BREVO_API_KEY, BREVO_FROM_EMAIL, CRON_SECRET
  - Cron job pg_cron attivi: settimanale (lunedì 08:00 UTC) + mensile (1° del mese 08:00 UTC)
- Provider email: cambiato da Resend a Brevo (free tier, 300 mail/giorno)
  - Resend richiedeva dominio verificato (libretto.daner.com non gestito da Vito)
  - Brevo permette sender @gmail.com verificato, invia a tutti gli utenti
  - Mail funzionante, arriva in spam inizialmente → risolvibile con "Non è spam" in Gmail
- CRON_SECRET: libretto-cron-2026-xK9mQ (hardcodato nei cron job SQL)
- Test manuale OK: {"sent":1} per manciaracina92@gmail.com

NOTE OPERATIVE:
  - Se la mail torna in spam: verificare un dominio custom su Brevo per DMARC corretto
  - Per aggiornare la funzione: modificare src, git push → deploy automatico via GitHub Actions
  - Per testare manualmente dal SQL Editor:
      select net.http_post(
        url := 'https://marvmbewsgxrabirugkk.supabase.co/functions/v1/send-report',
        headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer libretto-cron-2026-xK9mQ'),
        body := '{"type":"weekly"}'::jsonb
      );

--- STORICO ---
10/07/2026 — Sessione 6: Mail riepilogo automatiche (v22.4)
SAL:
- Creata Supabase Edge Function TypeScript: supabase/functions/send-report/index.ts
  - Parametro type = 'weekly' | 'monthly'
  - Weekly: ultimi 7 giorni (da ieri a 7 gg fa); lanciato ogni lunedì alle 08:00 UTC
  - Monthly: ciclo stipendio precedente (da stipendio(n-1) a stipendio(n)-1gg, per-utente)
  - Legge tutti gli utenti da auth.users (service_role) + libretto_user_state
  - Salta utenti senza transazioni nel periodo
  - Calcola: totale entrate/uscite/saldo, spese per categoria, top 5 spese singole
  - Monthly: aggiunge colonna Budget + % usato per categoria (da state.budgets)
  - Invia mail HTML responsive via Resend API
  - Protetta da CRON_SECRET (Authorization: Bearer <secret>)
- Creato SQL setup: supabase/sql/cron_setup.sql
  - Abilita pg_cron + pg_net
  - Salva CRON_SECRET come parametro database (app.cron_secret)
  - Schedula 2 cron job che chiamano la Edge Function via http_post
  - Include comandi utili per manutenzione e test manuale

PREREQUISITI PER IL DEPLOY:
  1. Creare account su resend.com
  2. Ottenere API key Resend
  3. Verificare dominio mittente su Resend (o usare onboarding@resend.dev per test)
  4. Installare Supabase CLI: npm install -g supabase
  5. Login CLI: supabase login
  6. Link progetto: supabase link --project-ref marvmbewsgxrabirugkk
  7. Impostare secrets:
       supabase secrets set RESEND_API_KEY=re_xxxx
       supabase secrets set RESEND_FROM_EMAIL="Libretto <noreply@tuodominio.com>"
       supabase secrets set CRON_SECRET=una-stringa-casuale-lunga
  8. Deploy function: supabase functions deploy send-report --no-verify-jwt
  9. Eseguire supabase/sql/cron_setup.sql nel Supabase SQL Editor
     (ricordare di sostituire 'CAMBIA_QUESTO_VALORE' con il CRON_SECRET impostato)
  10. Testare con: supabase functions invoke send-report --body '{"type":"monthly"}'

--- STORICO ---
09/07/2026 — Sessione 5: Form Feedback utenti (v22.3)
SAL:
- Aggiunta pagina "💬 Feedback" visibile a tutti gli utenti (non in ADMIN_ONLY_PAGES)
- Form con campi: Tipo (bug/feature/miglioramento/altro), Titolo, Descrizione, Comportamento atteso
- Al click "Invia feedback" si apre il client mail con subject e body già compilati
- Il body include una sezione "PROMPT CLAUDE CODE" strutturata per agevolare la creazione di richieste tecniche
- APP_VERSION aggiornata a 22.3-feedback-form

--- STORICO ---
09/07/2026 — Sessione 2: Migrazione a Vite + Deploy GitHub Pages
SAL:
- Introdotto Vite come build tool e dev server (npm + vite)
- Creato package.json con script dev / build / preview / deploy
- Creato vite.config.js e .gitignore
- Lavorato su branch feature/ dedicati, poi merge su develop → main
- Spostati file statici (manifest, icona, sw.js) nella cartella public/
- Estratto l'intero blocco <script> inline da index.html in src/main.js (1776 righe)
- index.html è ora pulito: solo struttura HTML (57 righe) + due tag <script>
- Configurato vite.config.js con base path dinamico (/Libretto/ in build, / in dev)
- App live su: https://fandore.github.io/Libretto/
- I test locali si eseguono con "npm run dev" su http://localhost:5173
- Il deploy in produzione si esegue con "npm run deploy" da locale

PROBLEMI RISCONTRATI E SOLUZIONI:
  PROBLEMA: Dopo il primo deploy, GitHub Pages serviva il vecchio index.html
  (con src="/src/main.js") invece del file buildato da Vite (con gli asset hashati).
  CAUSA: Il workflow GitHub Actions (.github/workflows/deploy.yml) sembrava girare
  correttamente ma il deployment effettivo continuava ad essere servito dal vecchio
  sistema Jekyll, che aveva deployato il sorgente grezzo invece della cartella dist/.
  SOLUZIONE: Abbandonato l'approccio GitHub Actions in favore del pacchetto npm
  "gh-pages". Con "npm run deploy" Vite builda il progetto e gh-pages pubblica
  automaticamente la cartella dist/ sul branch gh-pages. Su GitHub Pages è stata
  impostata la sorgente: "Deploy from branch → gh-pages → /(root)".

--- STORICO ---
09/07/2026 — Sessione 3: Modularizzazione Fase 1 + fix Supabase redirect
SAL:
- Fix bug registrazione: la mail di conferma Supabase reindirizzava a fandore.github.io (404)
  invece di fandore.github.io/Libretto/. Aggiunto emailRedirectTo nella chiamata signUp.
  Aggiornato anche il dashboard Supabase (Site URL + Redirect URLs).
- Modularizzazione Fase 1: estratti da src/main.js due nuovi moduli:
  - src/constants.js → DEFAULT_CATEGORIES, ACCT_COLORS, EMOJI_CHOICES, COLOR_CHOICES, MONTHS_IT
  - src/utils.js     → 16 funzioni pure (uid, isoToday, addDays, fmtEUR, fmtDate, escapeHTML,
                        lastDayOfMonth, clampDay, dayStart, dateKey, parseISODate, nowIso,
                        fmtDateTime, isTransfer, monthKeyFromDate, addMonths)
- main.js ora importa da questi moduli; ridotto da 1776 a ~1745 righe (pulizia strutturale).
- App testata in locale (npm run dev) e deployata su GitHub Pages — tutto funzionante.

--- STORICO ---
09/07/2026 — Sessione 4: Feature ricorrenti + cashflow (v22.2)
SAL:
- Aggiunta frequenza "Bimestrale" nel form movimenti (Mensile/Settimanale/Bimestrale/Annuale)
- Proiezione cashflow: spese bimestrali/annuali appaiono solo nei mesi in cui cadono
  (usa recurringNextDate per determinare i mesi corretti; fallback a equivalente mensile)
- Proiezione cashflow: entrate = solo media stipendi storici, non tutto il reddito
  (bonus e entrate una tantum non vengono proiettate nel futuro)
- monthlyFixedTotal() ora usa recurringMonthlyEquivalent() per tutte le frequenze
- Aggiunta funzionalità "Ferma/Riattiva ricorrenti":
  - state.stoppedRecurrings[] registra le voci manualmente fermate
  - Nel Dashboard → Riepilogo spese ricorrenti: tasto ⏹ per fermare ogni voce
  - Le voci fermate appaiono in fondo con tasto ▶ Riattiva
  - Le voci fermate sono escluse da proiezioni e totali
- APP_VERSION aggiornata a 22.2-bimestrale-salary-cashflow

--- STORICO ---
08/07/2026 — Sessione 1: Pulizia CSS
- Spostato lo <style> inline in css/app.css, eliminando ~100 righe da index.html

--- NEXT STEPS ---
1. ✅ Migrazione a Vite completata
2. ✅ Deploy su GitHub Pages via gh-pages branch
3. ✅ Modularizzazione Fase 1: src/constants.js + src/utils.js
4. ✅ Frequenza bimestrale/annuale per spese ricorrenti (ChatGPT req. 1)
5. ✅ Entrate non-stipendio escluse dai cashflow (ChatGPT req. 2)
6. ✅ Stop/Riattiva spese ricorrenti
7. ✅ Pagine visibili solo all'admin (req. 3)
8. ✅ Form feedback utenti con mailto: strutturato (req. 4)
9. ✅ Backlog approvato in coda a ChatGPT_requests.txt (req. 5)
10. Backlog (vedi ChatGPT_requests.txt):
    - ✅ req. 6: Mail riepilogo settimanale + mensile (sessione 6)
    - req. 7: Importi ricorrenti variabili (media ultimi 3 per bollette)
    - req. 8: Modularizzazione Fase 2 — src/state.js + src/storage.js
