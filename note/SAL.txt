09/07/2026 — Sessione 2: Migrazione a Vite
SAL:
- Introdotto Vite come build tool e dev server (npm + vite)
- Creato package.json con script dev / build / preview
- Creato vite.config.js e .gitignore
- Creati branch feature/vite-setup (da develop) per lavorare in isolamento
- Spostati file statici (manifest, icona, sw.js) nella cartella public/
- Estratto l'intero blocco <script> inline da index.html in src/main.js (1776 righe)
- index.html è ora pulito: solo struttura HTML (57 righe) + due tag <script>
- App testata su http://localhost:5173 — tutto funzionante
- I test vengono eseguiti su LocalHost con "npm run dev"

--- STORICO ---
08/07/2026 — Sessione 1: Pulizia CSS
- Spostato lo <style> inline in css/app.css, eliminando ~100 righe da index.html

--- NEXT STEPS ---
1. Fare merge di feature/vite-setup su develop, poi su main
2. Suddividere src/main.js in moduli separati:
   - src/constants.js  → DEFAULT_CATEGORIES, colori, emoji
   - src/state.js      → oggetto state e funzioni derivate
   - src/storage.js    → localStorage + Supabase (cloud sync)
   - src/utils.js      → funzioni di utilità (date, formattazione, ecc.)
   - src/pages/        → una funzione renderXxx per ogni sezione
   - src/components/   → modal, toast, sidebar
3. Aggiornare src/main.js come entry point che importa i moduli
4. Valutare deploy automatico su GitHub Pages da branch main
